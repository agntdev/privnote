[
  {
    "name": "/cancel resets any active flow",
    "steps": [
      {
        "send": { "text": "/cancel" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Cancelled. Tap /start to begin again." }
          }
        ]
      }
    ]
  }
]